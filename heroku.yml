setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    release:
      target: builder
    web: ./web/Dockerfile
    app: ./app/Dockerfile
    db: ./db/Dockerfile