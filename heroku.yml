# setup:
  # addons:
  #   - plan: heroku-postgresql
  #     as: DATABASE
build:
  docker:
    web: ./docker/web/Dockerfile
    app: ./docker/app/Dockerfile
    db: ./docker/db/Dockerfile
run:
#php artisan serveのphpがない
  web: /bin/sh -c "envsubst < /etc/nginx/conf.d/default.conf > /etc/nginx/conf.d/default.conf && exec nginx -g 'daemon off;'"
  app: php artisan migrate
  # webのbundleってやつ原因じゃねbundle not foundとか確か出てるし