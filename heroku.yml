setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    release:
      dockerfile: Dockerfile
      target: builder
    web: ./web/Dockerfile
    app: ./app/Dockerfile
    db: ./db/Dockerfile
run:
  web: bundle exec puma -C config/puma.rb